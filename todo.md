# SDSM 개발 TODO (관리자 + 조사자)

> ⚠ **2026-07-22부터 작업 계획은 `docs/` 폴더로 이관되었다.**
> 실제 작업 지시는 `docs/00_MASTER_PLAN.md` 와 각 단계 문서(`docs/01~06`)를 따른다.
> 이 문서는 **지금까지의 상세 이력·근거 보관용**으로만 유지한다.

> 최종 갱신: 2026-07-16 · 이 문서는 작업 진행에 따라 계속 갱신됩니다.
> 범례: `[x]` 완료 · `[~]` 진행중 · `[ ]` 예정

---

## A. 공통 (관리자·조사자)

- [ ] **엑셀 지수계산식 검수** — 조사자 제출 엑셀에 들어있는 수식(FAI·BMI·DI·H'·R1·J'·수질 등급·서식수변 평가)이 내부 코드 로직(`GB` / `EcologyCalculator` / `BenthosCalculator`)과 동일한지 셀 수식 단위로 대조.
  - 확인됨: 관리자 프로그램은 엑셀의 지수 "값"은 무시하고 원자료로 **재계산**함(BulkFish:379, BulkBenthos:362~366, Editor에서도 재계산).
  - 남은 일: 엑셀 "수식 자체"가 코드와 같은 정의인지 검증(반올림·결측 처리·조사불가 처리 포함).
- [ ] **SDSM_Core 공용 라이브러리 추출** — 검증·계산·엑셀양식·테마를 관리자/조사자가 공유(현재 조사자 앱 내부 `Helpers`/`Behaviors`/`Ecology`에 임시 배치).

---

## B. 관리자 프로그램

- [ ] **지도 지점 클릭 줌 제어** — 클릭 시 현재 확대 비율을 확인하고, **클러스터(그룹)가 풀리는 단계까지만** 확대되도록 줌 레벨을 조정(과도한 확대 방지).
- [ ] **조사지점 관리 기능** — 관리자가 **매년 지점명 수정·병합·분야(분류군) 선택** 등을 관리. 설정 창에 배치하되 **평상시 비활성화, 관리자 권한에서만 활성화**.
- [ ] **조사지점 그리드 구분 체계 정비** — `SiteDivision` 기준 4단계 구분 정착:
  - 프로젝트명(`Project`: 방류하천 / 생태현황)
  - 사업장(`Workplace`: 기흥 / 온양 / 화성 …)
  - 과업 대표하천(`WorkplaceRiver`: 오산천 / 곡교천 / 원천리천 …)
  - 지점명(`WorkplaceSite`: 오산천1, 곡교천1 …)

---

## C. 조사자 프로그램 (SDSM_Surveyor)

### C-1. 완료 (현재까지)
- [x] 솔루션 뼈대 · DI(Scrutor) · Telerik Windows11 테마 · 오프라인 로컬 저장(AppData)
- [x] **초성 자동완성** (ChosungHelper + ChosungFilteringBehavior, 알고리즘·Telerik API 검증)
- [x] **어류** 입력화면 + 실시간 FAI(점수/등급)
- [x] **저서동물** 입력화면 + 실시간 DI·H'·R1·J'·BMI
- [x] **조류 / 포유류 / 양서파충류** 관찰 입력화면(흔적·개체수·총 종수)
- [x] **수질** 입력화면 (패턴 C: 항목 폼 + 항목별 등급 자동산정, `WaterQualityCalculator`)
- [x] **서식수변** 입력화면 (패턴 C: 평가항목 10개 → 평가점수/등급, `HabitatEvaluator`)
- [x] **7개 분류군 입력화면 전체 완료** (어류·저서·조류·포유류·양서파충류·서식수변·수질)
- [x] 조사자용 `CLAUDE.md`·`design.md` 작성 (기존 분석문서는 이 둘로 통합·폐기)

### C-3. 대분류 · 조사지점 (핵심)
- [ ] **대분류(방류하천 / 생태현황) 구분 입력폼** — 최상단에서 사업 구분 선택 후 그에 맞는 입력·지점 목록 제공.
- [ ] **조사지점 선택 제한** — 조사자는 자유 입력이 아니라 **관리자가 등록한 지점 번호만 선택**.
- [ ] **지점 코드 변환** — 조사자가 `ST1`, `ST2`로 입력해도 DB에는 실제 지점명(`곡교천1`, `곡교천2`)으로 변환 저장.
- [ ] **연도별 지점 이력 반영** — 엑셀 `조사지점` 시트의 연도별 지점 변경 내용을 기준 데이터로 사용.
- [ ] **좌표 → 외부 지도 연동** — 조사지점 좌표 클릭 시 외부 지도(네이버/구글 등)로 자동 연결(조사자 인지 지원).

### C-4. 입력 범위 정책 — "자동계산 외 전부 입력"
> 정정(2026-07-22): "단순화"는 **항목 축소가 아니라 반복입력 축소**를 의미. 필드는 관리자와 동일하게 전부 두되, 반복 타이핑만 줄인다.
> 아래 목록은 관리자 실제 코드(`SDSM_Models\*.cs`, `GB.cs`)에서 확정. 자동계산 필드만 입력에서 제외.

**어류 (Fish.cs)** — 현재 화면에 없는 필드 다수. 추가 필요:
- 조사개황: 연도(SurveyYear), 연도차수, 지점명, 조사일, 대권역명, **중권역명**, 하천명, **하천유형**, **위도**, **경도**, 날씨, **조사기관**, 조사자
- 채집방법: **채집소요시간(분)**, 채집도구, **흐름상태**, 하천차수
- 서식지 하상구성(int%): **암반·콘크리트·진흙·모래·잔자갈·자갈·작은돌·큰돌** (8개 전부 신규)
- 서식처: **하천형태**, **흐름상태**
- 채집불가시: 특이사항(SurveyUnavailableReason), **비고(Note)**
- 비정상종: DE·EF·LE·TU (완료)
- 종그리드: 목·과·국명·학명·개체수
- 자동계산(제외): TotalIndividual/Species, M1~M7, FAIScore, FAIGrade, 종별 RankScorer

**저서동물 (Benthos.cs)** — 대량 추가 필요:
- 조사개황: 어류와 동일 13개(중권역·하천유형·위도·경도·조사기관 포함)
- 채집방법: **Surber net 30×30, 50×50, 드렛지, 에크만** (4개 신규)
- 서식처①: **유역이용·확인가능오염원·식생수피도·범람원이용·제방(좌안)·제방(우안)** (6개 신규)
- 서식지 하상구성: 8개(어류와 동일)
- 서식처②: **하천유형·하폭·수폭·평균수심·평균유속·기온·수온·흐름상태·투명도·냄새** (10개 신규)
- 기타: **특이사항·채집불가시**
- 종그리드: 문·강·목·과·국명·학명·개체수
- 자동계산(제외): DI·H'·R1·J'·BMIScore·BMIGrade, 종별 RankScorer

**조류 (Bird.cs)** — 자동계산 없음(전부 입력). ✅ **완료(2026-08-20)** — 학명·위도·경도·특이사항 추가, 조사개황 `SurveyMeta` 교체.
**포유류 (Mammal.cs)** — 자동계산 없음. ✅ **완료(2026-08-20)** — 흔적 12종 bool → `int?`(Trace1~12) 교체, 학명·위도·경도·특이사항 추가, 조사개황 `SurveyMeta` 교체.
**양서파충류 (AmphibianReptile.cs)** — 자동계산 없음. ✅ **완료(2026-08-20)** — 흔적 6종 bool → `int?`(Trace1~6) 교체, 학명·위도·경도·대분류·중분류·특이사항 추가, 조사개황 `SurveyMeta` 교체.
**수질 (WaterQuality.cs)** — 측정 9개 + **추가항목 14개**(TN·EC·Cl·SO4·Cu·Zn·Cr·탁도·클로로필a·수온·수심·유속·초당/일당유량). 등급 8종만 자동계산. ✅ **완료(2026-08-20)** — 추가항목 14개 신규 입력, [측정항목]/[추가항목] 2그룹 열배치, 조사개황 `SurveyMeta` 교체.
**서식수변 (HabitatWaterEdge.cs)** — 평가항목 10개(완료) + 특이사항. 평가점수/등급만 자동계산. ✅ **완료(2026-08-20)** — 비고(Note) 추가, 조사개황 `SurveyMeta` 교체(조사불가 사유는 등급 산정용이므로 분류군 고유 유지).

- [ ] (참고) 기존 엑셀 방식은 이 프로그램 완성 후 **폐기 예정**. 엑셀 내보내기는 아래 두 목적으로만 유지.

### C-4b. 반복입력 단순화 (엑셀식 편의)
- [x] **복사/붙여넣기(클립보드)** — 5개 종 그리드에 `ClipboardCopyMode=Cells`(헤더 제외) + `ClipboardPasteMode=InsertNewRows`(행 자동 확장). ※ enum명은 이 Telerik 버전 실DLL로 검증(멤버: None/InsertNewRows/OverwriteWithEmptyValues/Repeat). 복사시 `All`은 헤더('국명')까지 포함해 붙여넣기 오류를 유발 → `Cells`로 교정.
- [x] **국명 자동매칭** — 붙여넣기·직접입력으로 국명만 채워진 행을 공식 종목록과 대조해 학명·길드·오탁치를 자동 연결(`ResolveSpecies`).
- [x] **빠른 추가 바(어류 기준 구현)** — 그리드 위 `종 초성검색 + 개체수 + Enter/추가`. 종 선택→개체수 칸 자동 이동, 추가 후 검색창 자동 복귀(코드비하인드 포커스 처리). 그리드 셀 편집의 두 번 클릭·첫 글자 손실을 우회하는 주 입력 경로.
- [x] **빈 행 정리 버튼** — 국명·개체수가 모두 빈 행 제거(`PruneEmpty`). 초기 빈 행 자동추가 제거.
- [x] **조사개황 1회 입력 → 접기** — 상단 조사개황을 `RadToggleButton` 접이식으로 구현(어류). 접으면 종입력 공간 확보.
- [x] **빠른 추가 바·접이식 조사개황·전체필드 나머지 분류군 복제** — **7개 분류군 전부 완료(2026-08-20)**. 어류·저서동물에 이어 관찰형 3종(조류·포유류·양서파충류)에 빠른추가 바·Ctrl+V 직접파싱·빈행정리 이식, 수질·서식수변은 접이식 조사개황 적용. 관찰형 붙여넣기 열 순서 = 그리드 열 순서(문서화: `02_FEATURE_SCREENS.md`).
- [ ] **채우기(Fill-down)** — 선택 셀을 아래로 반복 채우기(Ctrl+D식). RadGridView 기본 미지원 → 커스텀 명령 추가 예정.
- [ ] **지점별 값 복사** — 현재 조사개황을 통째로 복사해 다음 지점 입력의 출발점으로 사용(지점명·좌표만 교체).
- [ ] **이전 조사자료 복사 후 수정** — 저장된 이전 회차/지점 draft를 불러와 개체수만 갱신(연도차수·조사자 등은 유지). ※ draft 스키마가 C-4 필드확장 후 바뀌므로 확장 완료 후 구현.

### C-5. 엑셀 내보내기 (두 종류)
> 양식 확정: 관리자 코드(Bulk*ControlViewModel) + 실제 제출 엑셀 대조. **라이브러리 = Telerik 문서처리**(관리자와 동일, 조사자 bin에 이미 포함 → 100% 호환). 어류/저서/수질/서식수변=전치(필드=행, 레코드=열 Q/M/F/F), 조류/포유류/양서파충류=일반(1행=1기록). 자동계산(FAI·BMI·등급 등)은 관리자 재계산이라 내보내지 않음. **파일명에 방류하천/생태현황 키워드 필수**(대분류를 SurveyMeta에 추가). 어류 종=행79+, 목/과/국명/학명=L/M/N/O, 개체수=Q. **템플릿에 '조사지점' 시트 존재 → C-3 조사장소 번호 원천.**
- [~] **① 일괄입력용** — 관리자 Import 필수자료만. **어류·저서동물 완료**(`FishExcelExporter`='어류_입력', `BenthosExcelExporter`='입력' 시트, 전치). 관측된 종(개체수>0)만 내보냄. 이상치 경고는 내보내기 막지 않음(`CanExport` 수정). 남음: 수질·서식수변(전치), 조류·포유류·양서파충류(일반). 수질 확장항목은 C열 라벨 검색 방식 주의.
  - ⚠ **관리자 일괄입력 취합 테스트는 맨 마지막에** 일괄 수행(사용자 요청).
- [~] **② 보고서·기록용(재설계·주력)** — 목적 재정의: 기존 엑셀은 보고서용 계산식을 다 품어 복잡+파일 폭증(연 30개+). 조사자 앱이 입력·계산을 맡고, 내보내기는 **사람이 읽는 깔끔한 워크북 + 건강성평가 계산과정·결과·등급**. **분류군별 개별 파일**(사용자 선택). **어류 완료**(`FishReportExporter`: 시트 조사개황/출현종/건강성평가(FAI M1~M8 산출값·배점·총점·등급)). FAI 상세는 `EcologyCalculator.CalculateFaiDetail`로 노출. 어류 화면 버튼 = [일괄입력용](레거시 전치) + [보고서 엑셀](주력). 남음: 저서(BMI·DI·H'·R1·J')·수질·서식수변·조류·포유류·양서파충류 보고서 시트.
  - 관리자 연동: 이 깔끔한 양식을 관리자가 **간편 가져오기**로 분류군 테이블에 반영(관리자 프로그램 후속 수정). 기존 전치 일괄입력은 병행 보존.
- [ ] **생물종별 개별 엑셀** 내보내기 지원.
- [ ] 내보내기 엑셀에 **구분 컬럼** 추가(관리자 설정의 프로젝트·사업장·지점 그리드 = `SiteDivision` 참조):
  - 프로젝트명(방류하천/생태현황) · 사업장(기흥/온양/화성) · 과업대표하천(오산천/곡교천/원천리천) · 지점명

### C-6. UI
- [x] **디자인 기준 확정** — 관리자 디자인 시스템(색·타이포·치수·레이아웃) + 공개 UI 모범사례를 통합해 `design.md` 단일 기준 수립. **라이선스 대상(삼성 로고·브랜드 남색·SamsungOne 서체)은 제외.**
- [x] **디자인 적용 1단계(토큰·셸)** — `Styles/Colors.xaml`(강조 `#0381FE`, 셸 `#1E232E` 계열, 분류군색, 차트색) + `Styles/Styles.xaml`(폰트 14.5·타입스케일 26/19/15/11.5, 치수 40/1240, `CardBorderStyle` padding 20, 셸 캡션버튼) 신설 → App.xaml 병합. App 초기화에서 **팔레트 Accent를 앱 강조색으로 통일**(기존 화면의 `AccentBrush` 일괄 반영) + 폰트·테두리 값 관리자 계승. MainSurveyWindow 캡션바를 **딥네이비 50px 셸**로 정비.
- [x] **디자인 적용 2단계(탭·핵심 서식)** — 분류군 색 표시등이 있는 **탭 헤더 템플릿**(`TaxonTabHeaderTemplate`) + 탭 크기/여백 통일. 어류·저서·서식수변·수질의 섹션 헤더·핵심 수치·등급을 `SubHeaderTextStyle`/`ValueTextStyle`/`GradeTextStyle`/`FormLabelStyle`로 교체.
  - ⚠ StyleManager 테마에서는 **RadTabItem에 Style 통째 지정 금지**(테마 템플릿 벗겨짐) → HeaderTemplate·MinWidth·Padding만 직접 지정.
- [x] **디자인 적용 3단계(여백·라벨 통일)** — 7개 화면 헤더 카드 여백을 padding 14로 통일, 수질·서식수변의 로컬 라벨 스타일을 공용 `FormLabelStyle` 상속으로 교체.
- [x] **디자인 적용 4단계(관리자 화면과 통일)** — 실제 화면 캡처 대조 후 반영:
  - **분류군 탭을 관리자 통계 화면과 동일한 세그먼트 형식으로 교체.** `RadTabControl` → `RadioButton` 기반 `TaxonTabStyle`(7등분 균등폭·선택 시 흰 배경+상단 강조선·분류군 색 표시등). Telerik 테마와 충돌하지 않는 순수 WPF 템플릿이라 StyleManager 제약을 받지 않음.
  - **타이틀 중앙 정렬** — 관리자와 동일한 배치(좌: 아이콘·버전 / 중앙: 제목 / 우: 창 버튼).
  - **관리자 '신규' 다이얼로그식 폼 토큰 신설** — `FieldLabelBorderStyle`(라벨 좌·입력 우), `GroupLabelBorderStyle`(세로 그룹 라벨: 조사개황·채집방법 등), `RequiredMarkBrush`. ※ 적용은 5단계.
- [ ] **디자인 적용 5단계(입력 폼 재구성)** — 각 화면의 공통 기본정보를 관리자식 **세로 그룹 라벨 + 라벨좌/입력우** 배치로 교체하되, 관리자의 3열 빽빽한 폼은 **조사자에게 필요한 항목만 남겨 단순화**.
- [ ] (선택) 관찰형 3종의 인라인 라벨을 `FormLabelStyle`로 개별 교체 — 시각차 미미(14.5→15px)하고, **일괄 치환은 아이콘의 동일 `Foreground` 패턴까지 잡아 TargetType 오류를 유발**하므로 개별 편집 필요. 우선순위 낮음.
- [ ] (보류) 삼성 브랜드 자산·서체 **공식 승인 시** `design.md` §3·§4 갱신 후 반영.

### C-8. 화면 결함 수정 (실행 캡처 기반)
- [x] **빈 입력인데 지수가 표시되던 문제** — 어류 FAI가 자료 0건에서도 `37.5 D`, 저서 DI가 `1.000`으로 표시됨(M1~M8 기본점·0나눗셈 결과). 유효 개체 0이면 계산하지 않고 `-` 표시하도록 수정. 저서 DI/H'/R1/J'는 `double?`로 변경.
- [x] **Telerik 영문 문구 한글화** — `KoreanLocalizationManager` 신설(그리드 새 행 안내·필터 메뉴 등). 그룹 패널은 조사자에게 불필요하여 `ShowGroupPanel="False"`로 숨김.
- [x] **그리드 컬럼 폭** — 어류·저서: 국명 230 고정 / 학명 `*` / 개체수 110(기존엔 국명이 화면 대부분을 차지). 포유류·양서파충류: 흔적 체크 컬럼 헤더가 `포ㅎ`, `로드ㅋ`처럼 잘리던 문제 → 필터 아이콘 제거(`IsFilterable="False"`) + 폭 50/58/68로 확대.
- [x] **기본정보 정합성** — 어류에 누락된 **조사자** 필드 추가(`FishDraft`에도 반영), 연도차수 워터마크를 7개 화면 모두 `2024반기1차`로 통일.
- [ ] **수질·서식수변 화면의 빈 공간** — 항목이 상단 한 줄에만 몰려 있고 아래가 비어 있음. 5단계 폼 재구성 때 2열 배치로 정리.

### C-7. 지침 준수 보완 (`GUIDELINE_REVIEW.md` 근거)
- [x] **[높음] 어류 비정상종 입력** — 기형(DE)·지느러미손상(EF)·피부손상(LE)·종양(TU) 개체수 입력 추가 → FAI M8 실제 반영.
- [x] **[높음] 서식수변 드롭다운 + 좌/우안** — [별표5] 허용점수 드롭다운, 5개 항목(하도자연성·저수로하안공·제방하안재료·제외지·제내지) 좌/우안 자동 평균.
- [x] **[중] 입력 이상치 경고 (기준자료 기반)** — 관리자↔조사자 연동 완료:
  - 관리자: 설정 화면 **[기준자료 내보내기(JSON)]** → 종별 과거 개체수 min/max 산출(`ReferenceDataExporter`). 공유 모델 `SDSM_Models/ReferenceData`.
  - 조사자: `reference.json` 로드(`ReferenceRangeProvider`, AppData 우선·번들 대체) → 어류·저서 그리드에서 **과거 최대 초과/신규 종**을 비차단 경고(붉은 행+툴팁, 상태바 경고 건수).
  - 현재 번들: 실DB 산출본(어류 55종·저서 118종). 배포 시 관리자 최신본으로 교체.
  - 추가 여지: 지점·차수별 범위, 수질/서식수변 하드범위, 종목록 외 종 차단.
- [ ] **[중] FAI 대하천(표12) 지원** — 하천 규모(일반/대하천) 구분(M1·M2·M3·M7 기준 상이). 삼성 소하천은 영향 적음.
- [~] **[중] 종목록 정합/외부화** — 하드코딩(어류13·저서12 시드) 폐기. **관리자 DB 마스터를 `species.json`으로 외부화**: 어류253·저서803·조류169·포유류17·양서파충류22종. `SpeciesListProvider`가 AppData→번들 순 로드(reference.json과 동일), 파일 교체로 갱신. 공유 모델 `SpeciesCatalog`(SDSM_Models). 추후 [별표2/3] 대조·체크섬.

### C-7b. 종목록 관리 (조사자 앱 → 관리자 취합)
> 결정(2026-07-22): 조사자는 **조사자 앱만** 사용, 관리자 프로그램은 삼성 담당자 **취합용**. 따라서 종 추가·수정은 **조사자 앱에서** 하고, 내보내기를 타고 관리자로 올라가 취합된다(초기 species.json 배포 방향의 역방향).
- [ ] **로컬 사용자 종 overlay** — `%AppData%\SDSM_Surveyor\species_user.json`. 번들 `species.json`(마스터) 위에 병합 로드. 파일 교체(마스터 갱신) 시에도 사용자 추가분 보존.
- [ ] **종 추가·수정 화면** — 분류군별 국명·학명(+어류 길드, 저서 오탁치/가중치) 입력. 빠른 추가 바 옆 [+ 새 종 등록] 또는 설정의 종 관리.
- [ ] **사용자 추가분 표시·전달** — 추가/수정 종을 '사용자추가/신규'로 표기. 내보내기(C-5)에 포함 → 관리자 import 시 마스터 병합(취합). ※ 관리자 import가 신규종 수용하도록 연동 필요.
- 참고: 현장 즉시 기록은 이미 지원(자유입력 국명 → 신규종 경고). 위 기능은 이를 '정식 종'으로 승격·전달하는 경로.
- [ ] **[검토] 부착돌말 TDI·수변식생 RVI** 모듈 — 삼성 과업 범위 확인 후 추가 여부 결정.
- 참고: BMI·FAI(일반하천)·HRI·수질 등급 계산식은 지침/규정과 **일치 확인됨**.

---

### C-8b. 공통 조사개황 중앙화 (유지보수)
- [x] **공유 모델 `SurveyMeta`** — 모든 분류군 공통 조사개황(연도·연도차수·조사일·대권역·중권역·하천명·하천유형·지점·좌표·날씨·조사기관·조사자)을 한 클래스로. 각 분류군 VM은 `Meta` 하나만 보유.
- [x] **공유 컨트롤 `SurveyOverviewControl`** — 조사개황 UI를 한 번만 작성. 각 화면은 `<ctrl:SurveyOverviewControl DataContext="{Binding Meta}"/>`로 끼움. **연도차수 형식·라벨·드롭다운 전환을 이 한 곳에서 바꾸면 전 분류군 일괄 반영.**
- [x] **7개 분류군 전부 적용 완료(2026-08-20)** — 공통 13필드를 각 VM에서 제거하고 `Meta`로 대체, 임시저장 Draft도 `Meta.X`를 읽는다. 중복 속성 없음.
- [ ] (후속) 대권역·중권역·하천·하천유형을 드롭다운으로 전환 시 `SurveyMeta` 옵션 목록 + 컨트롤 콤보 교체 한 번. 목록은 관리자 등록 지점(C-3)과 연동.

### C-9. 지점 자료 보기·복제·관리 (조사 세션 / 자료함)
> 배경: 현재 저장소는 `drafts\{분류군}.json` **단일 슬롯**(덮어쓰기)이라 여러 지점을 못 담고 불러오기도 없음. 아래로 재설계.

**핵심 개념 — 조사 세션(SurveySession):** 저장 단위 = `대분류(방류하천/생태현황) + 연도차수 + 지점`. 하나의 세션이 **공통 조사개황(지점·좌표·조사자·날씨·조사기관 등) + 7개 분류군 데이터**를 함께 보관. (지금의 분류군별 단일 draft를 대체)

- [ ] **저장 구조 개편** — `drafts\{분류군}.json` → `sessions\{sessionId}.json`(다건) + `index.json`(목록·메타). sessionId = 대분류·연도차수·지점 조합. 오프라인 AppData 유지.
- [ ] **자료함 화면(보기)** — 저장 세션 목록: 대분류·연도차수·지점·최종수정·입력된 분류군·경고 건수. 정렬·검색. 상단바 또는 첫 화면(홈)에서 진입.
- [ ] **불러와 편집** — 세션 열면 7개 탭에 값 복원 → 이어서 수정.
- [ ] **복제 ① 지점 간 값 복사** — 현재 조사개황만 복사해 **새 지점** 시작점 생성(연도차수·조사자·날씨·조사기관 유지, 지점명·좌표만 교체, 종목록은 빈칸).
- [ ] **복제 ② 이전 차수 복사 후 수정** — 같은 지점의 지난 회차를 **통째로 복제**(종목록 유지) → **개체수만 갱신**. 반복 모니터링 최대 절감. ※ draft 스키마 확정(전체필드) 후 구현.
- [ ] **관리** — 삭제(확인), 지점·차수 변경(re-key), (C-3 연동) 조사자는 관리자 등록 지점만 선택·ST1→곡교천1 변환.
- [ ] **관리자와의 관계** — 로컬 세션은 조사자 작업본. 최종 산출은 **엑셀 내보내기(일괄입력용/열람용, C-5)** 로 관리자 마스터 DB에 전달. 자료함은 마스터가 아님.
- 선행조건: 7개 화면 전체필드 완성(C-4, 세션이 모든 분류군을 담으므로) → 세션 계층 구축.

---

### C-10. 비동기 동기화 (교환소 폴더)
> 결정: 서버·실시간 양방향 동기화는 보류(오프라인 우선·비용 과다·빈도 낮음). 대신 **공유/클라우드 폴더(교환소) + 파일 버전 안내 + 가져오기**로 비동기 동기화.
- [x] **설정 저장소** `SettingsStore`(`settings.json`) — 교환소 폴더 경로 저장.
- [x] **동기화 서비스** `SyncService` — 설치본(AppData→번들) vs 교환소의 `species.json`·`reference.json` **버전 비교**, **가져오기**(교환소→AppData, 다음 실행 시 반영).
- [x] **동기화 창** `SyncWindow` — 교환소 폴더 지정(찾아보기, .NET8 `OpenFolderDialog`)·버전 비교 표시·가져오기. 상단바 [동기화] 버튼(`CloudSyncOutline`)에서 진입.
- [ ] (후속) 내보내기(C-5) 완성 시 조사자 산출물을 교환소에 저장 → 관리자 취합. 사용자 종 overlay(C-7b) 포함.
- [ ] (후속) 시작 시 교환소 신버전 자동 감지 알림.

---

## D. 마일스톤 순서(권장)
1. ~~수질·서식수변 화면 완성 → 7개 분류군 입력화면 확보.~~ ✅ 완료
2. ~~디자인 관리자와 통일(세그먼트 탭·상단바·폼 토큰) + 실행 결함 수정.~~ ✅ 진행중 (4단계 완료, 5단계 남음)
3. **[다음] 입력 필드 전면 확장(C-4) — 어류→저서 순으로 관리자식 그룹폼 구현, 1개 완성 후 빌드 확인하고 나머지 복제.**
4. 반복입력 편의(C-4b) 나머지: Fill-down·조사개황 접기·지점복사·이전자료 복사.
5. 대분류·조사지점 선택/변환 체계(C-3).
6. 엑셀 내보내기(일괄입력용/열람용, 생물종별, 구분컬럼)(C-5).
7. 엑셀 지수계산식 검수(공통) + SDSM_Core 추출.
8. 관리자 지도 줌 제어.

---

### 변경 이력
- 2026-08-20(7): **ADMIN_TASKS §7-1·§6-2-1 완료**. HRI 편집화면이 평가항목 미입력 시 `0·E`로 저장하던 것을 `null`(`-`)로 통일 → **계산식 대조 1,388건 전부 일치**. FAI 공식 수정의 과거 데이터 영향 측정(읽기 전용): 503건 중 **등급 변화 6건**(전부 0종 레코드 `37.5·D`→`0·E`), 비정상종 입력이 0건이라 **M8 수정은 과거 데이터에 영향 없음**. 보고서 `docs/_recalc_impact_20260820.md`.
- 2026-08-20(6): **계산식 수정**(`12_CALC_FIX.md`) — 관리자·조사자 동시 적용. M8 실수 나눗셈(+0 나누기 가드로 DivideByZeroException 해소), 어류·저서 0종 → 점수 0·등급 E, `출현종 없음(0종)` 선언 토글 신설(내보내기 게이트 완화), BMI 음수 `Check` 통일, 관리자 HRI 편집화면 공백 제거. 저서 화면에서 조사불가 사유 변경 시 재계산이 안 되던 결함도 함께 수정. **재대조 1,388건 중 1,384 일치**(FAI·저서·수질·HRI 일괄 모두 불일치 0).
- 2026-08-20(5): **계산식 일치 검증**(`11_CALC_PARITY.md`). 관리자 `GB.cs` ↔ 조사자 `Ecology\` 를 경계값 중심 **1,379건 대조** → 일치 1,367 · 불일치 12. 수질 등급 8종·저서 지수는 완전 일치, HRI는 관리자 일괄입력(DB 저장) 경로와 완전 일치. 남은 차이는 **어류 0종 자료**(관리자 37.5·D / 조사자 null, 사용자 결정 대기), BMI 음수 등급명, 관리자 편집화면의 공백 미처리. 함께 발견: **M8 정수 나눗셈**(99.5%도 0%로 계산 → 지표 무력화, 수정 결정됨)과 **0 나눗셈 예외**(양쪽 동일). 결과는 `docs/_calc_parity_20260820.md`, 통합 기준은 `05_REFACTORING.md` §2-A. 코드는 고치지 않았다.
- 2026-08-20(4): **3단계 엑셀 내보내기 완성**(`03_FEATURE_EXPORT.md`). 공통 서식 헬퍼 `ExcelStyle` 추출, 7개 분류군 × [보고서용·일괄입력용] 총 14개 exporter 완비(신규 12개). 보고서 시트는 조사개황/출현종/건강성평가(관찰형은 출현종요약) 3종이며 지점명이 맨 왼쪽 + 헤더 필터. 저서 `BmiResult`·서식수변 `HriResult` 상세 결과 클래스를 추가해 계산과정(Σ(s·g·h)·Σ(g·h)·항목별 점수)을 수록. 각 exporter에 대화상자 없는 `Write(vm, path)` 분리 → 14개 파일 생성 후 재열람 38건 검증 통과. ⚠ 수질 확장항목 라벨에 단위를 붙이면 관리자 검색이 오인식(초당유량→EC)하므로 단위는 E열로 분리.
- 2026-08-20(3): **속(genus) 단위 보호종·교란종 등재 처리**(`10_SPECIES_MASTER.md` §7). `Trachemys spp.` 같은 속 단위 등재가 정확 일치 조인에 실패해 **붉은귀거북이 교란종으로 표시되지 않던 결함**을 수정. 학명이 `spp.`/`sp.`로 끝나면 속 이름만 비교해 그 속의 모든 종에 플래그를 부여하고, 속 단위로 붙은 종을 콘솔에 별도 보고한다. 양서파충류 교란종 4→5종, 그 외 플래그·어류·저서동물은 변화 없음.
- 2026-08-20(2): **관찰형 3종 공식 종목록 반영**(`10_SPECIES_MASTER.md`). 국립생물자원관 국가생물종목록 → 조류 609·포유류 125·양서파충류 65종(양서28+파충37)으로 교체, 각 종에 학명·목·과 + 멸종위기Ⅰ/Ⅱ급·천연기념물·생태계교란생물 조인. `SpeciesCatalog`의 관찰형 3종을 `List<string>`→`List<ObservedSpecies>`로 바꾸고 **구형 파일 하위 호환** 유지. 3개 화면에 종 선택·붙여넣기 자동 채움(수정 가능)·목/과 열·보호종/교란종 구분 표기 추가. 변환기 `tools/build_species_json.py` 저장소에 포함(재실행 가능). 어류·저서동물 종목록은 그대로 보존.
- 2026-08-20: **2단계 남은 5개 화면 전체 필드 확장 완료**(`02_FEATURE_SCREENS.md`). 조류·포유류·양서파충류·수질·서식수변을 어류·저서 수준으로 정리: 흔적 bool→`int?`(포유류 12·양서파충류 6), 기록별 위도·경도·학명·특이사항 추가, 수질 추가항목 14개 신규, 7개 분류군 모두 공통 `SurveyMeta`·관찰형 3종 빠른추가/Ctrl+V/빈행정리 적용. AutoFilter 이슈 해결 확인(`FilterRange` 속성 대입 가능). 빌드 통과 + 7개 탭 실행 확인.
- 2026-07-22: 실행화면(조사자·관리자) 캡처 대조. **디자인 4단계**(관리자 세그먼트 탭·중앙 타이틀·관리자식 폼 토큰) + **실행 결함 수정**(빈 입력 지수 `-`, 영문문구 한글화, 컬럼폭, 조사자 필드) 반영. **방향 정정**: "단순화=반복입력 축소"로 재정의, 자동계산 외 전부 입력 원칙 확정. 관리자 코드 기준 **분류군별 전체 필드 목록 확정**(C-4). 반복입력 편의(C-4b) 착수: 클립보드 복사/붙여넣기·국명 자동매칭 구현.
- 2026-07-16: 최초 작성. 조사자 어류/저서/조류/포유류/양서파충류 화면 완료 반영, 신규 요구사항(지점 관리·변환, 엑셀 내보내기 2종, 삼성 UI, 지도 줌) 추가.
- 2026-07-16(2): **수질·서식수변 화면 완료 → 7개 분류군 전체 완성.** 조사자용 `CLAUDE.md`·`design.md` 작성, `INPUT_SCREENS_ANALYSIS.md` 폐기. 다음 마일스톤 = 대분류·조사지점 체계.
- 2026-07-16(3): **수생태계 건강성 평가 지침 대조 검토**(`GUIDELINE_REVIEW.md`). BMI·FAI(일반하천)·HRI·수질 계산 일치 확인. 보완항목(어류 비정상종 입력, 서식수변 드롭다운·좌우안, 이상치 경고, FAI 대하천, 종목록 정합) → C-7 추가.
