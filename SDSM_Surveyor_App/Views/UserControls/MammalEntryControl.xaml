<UserControl x:Class="SDSM_Surveyor_App.Views.UserControls.MammalEntryControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation"
             xmlns:icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
             xmlns:core="clr-namespace:SDSM_Surveyor_App.Behaviors"
             xmlns:ctrl="clr-namespace:SDSM_Surveyor_App.Views.UserControls"
             xmlns:vm="clr-namespace:SDSM_Surveyor_App.ViewModels"
             mc:Ignorable="d" d:DesignWidth="1224" d:DesignHeight="540"
             d:DataContext="{d:DesignInstance vm:MammalEntryViewModel}">

    <!-- ══ 관찰형 기준 화면 — 포유류 22열 (06_DESIGN_REBUILD §5-2-2) ═══════════════
         22열을 **하나도 줄이지 않는다**(§3-6). 폭을 최소 실용폭까지 깎아도 1,504 px 라
         1280 창의 가용폭 1,224 에 280 px 모자란다 → 구조를 바꾼다.

           1행 : 종 식별 5 + 흔적계(읽기전용) + 위도·경도·특징·특이사항·구분  = 11열
           2행 : 흔적 12칸  (RowDetails 상시 표시 — 접거나 감추지 않는다)

         1행 고정 합 1,096 + 특징 128 = 1,224 로 1280 창에 정확히 들어간다.
         ⚠ 표시 행이 6행으로 줄어드는 것은 **승인된 대가**다(§5-7).
            행이 적게 보인다는 이유로 흔적 열을 접는 구조로 되돌아가지 말 것.

         Tab 순서(스파이크로 실측) :
           일반명 → … → 구분 → [2행] 포획 → … → 기타 → 다음 행 일반명
         ══════════════════════════════════════════════════════════════════════════ -->
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>  <!-- ① 빠른 추가 -->
            <RowDefinition Height="*"/>     <!-- ② 관찰 그리드(2단 행) -->
            <RowDefinition Height="Auto"/>  <!-- ③ 상태 · 액션 -->
        </Grid.RowDefinitions>

        <!-- ① 빠른 추가 : 개체수는 '관찰'(Trace2) 흔적으로 들어간다 -->
        <ctrl:QuickAddBar Grid.Row="0" Margin="0 0 0 8"
                          SearchWatermark="종명 초성 검색 (예: ㄱㄹㄴ)"
                          CountWatermark="관찰 개체수"
                          Hint="개체수는 '관찰' 흔적에 들어갑니다 · 나머지 흔적은 아랫줄에서 입력 · 엑셀 Ctrl+V 가능"/>

        <!-- ② 관찰 그리드 : 설정·Ctrl+V·보호종 강조는 ObservationGrid 가 소유한다 -->
        <ctrl:ObservationGrid Grid.Row="1" Rows="{Binding Entries}">
            <!-- ── 1행 11열 · 고정 합 1,096 + 특징 128 = 1,224 (1280 창 가용폭) ──────── -->
            <telerik:GridViewDataColumn Header="일반명" Width="168" DataMemberBinding="{Binding SpeciesKo}">
                <telerik:GridViewDataColumn.CellEditTemplate>
                    <DataTemplate>
                        <telerik:RadAutoCompleteBox SelectionMode="Single"
                            WatermarkContent="초성 검색 (예: ㄱㄹㄴ)"
                            DisplayMemberPath="SpeciesKo" TextSearchPath="SpeciesKo" TextSearchMode="Contains"
                            SelectedItem="{Binding SelectedSpecies, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                            ItemsSource="{Binding DataContext.SpeciesListSource, RelativeSource={RelativeSource AncestorType=UserControl}}">
                            <telerik:RadAutoCompleteBox.FilteringBehavior>
                                <core:ChosungFilteringBehavior/>
                            </telerik:RadAutoCompleteBox.FilteringBehavior>
                        </telerik:RadAutoCompleteBox>
                    </DataTemplate>
                </telerik:GridViewDataColumn.CellEditTemplate>
            </telerik:GridViewDataColumn>

            <telerik:GridViewDataColumn Header="학명" Width="150" DataMemberBinding="{Binding SpeciesEn}"/>
            <telerik:GridViewDataColumn Header="목" Width="78" DataMemberBinding="{Binding OrderKo}"/>
            <telerik:GridViewDataColumn Header="과" Width="78" DataMemberBinding="{Binding FamilyKo}"/>
            <telerik:GridViewDataColumn Header="관찰지유형" Width="104" DataMemberBinding="{Binding ObservationSite}"/>

            <!-- 흔적계 : 입력 항목이 아니라 피드백(§3-4). 2행을 보지 않아도 흔적 유무를 안다. -->
            <telerik:GridViewDataColumn Header="흔적계" Width="66" IsReadOnly="True" IsFilterable="False"
                                        TextAlignment="Right" DataMemberBinding="{Binding TraceSum}"
                                        ToolTipService.ToolTip="흔적 12칸의 합계(자동)"/>

            <telerik:GridViewDataColumn Header="위도" Width="92" IsFilterable="False"
                                        DataMemberBinding="{Binding Lat}" DataFormatString="{}{0:N6}" TextAlignment="Right"/>
            <telerik:GridViewDataColumn Header="경도" Width="92" IsFilterable="False"
                                        DataMemberBinding="{Binding Lng}" DataFormatString="{}{0:N6}" TextAlignment="Right"/>
            <telerik:GridViewDataColumn Header="특징" Width="*" MinWidth="120" DataMemberBinding="{Binding Feature}"/>
            <telerik:GridViewDataColumn Header="특이사항" Width="150" DataMemberBinding="{Binding Note}"/>
            <telerik:GridViewDataColumn Header="구분" Width="118" IsReadOnly="True"
                                        DataMemberBinding="{Binding ProtectionText}"
                                        ToolTipService.ToolTip="공식 종목록의 법정보호종·생태계교란생물 표기(자동)"/>

            <!-- ── 2행 : 흔적 12칸 (상시 표시) ─────────────────────────────────────── -->
            <ctrl:ObservationGrid.RowDetails>
                <DataTemplate>
                    <Border Background="{StaticResource Row2LineBrush}" Height="{StaticResource GridRow2LineHeight}">
                        <StackPanel Style="{StaticResource RowDetailsPanelStyle}">
                            <TextBlock Text="╰▸ 흔적" Width="66" Style="{StaticResource RowDetailsTagStyle}"/>
                            <TextBlock Text="포획" Style="{StaticResource RowDetailsLabelStyle}"/>
                            <telerik:RadWatermarkTextBox Width="{StaticResource RowDetailsCellWidth}" Margin="0 0 8 0" WatermarkContent="·"
                                                         Text="{Binding Trace1, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, TargetNullValue=''}"/>
                            <TextBlock Text="관찰" Style="{StaticResource RowDetailsLabelStyle}"/>
                            <telerik:RadWatermarkTextBox Width="{StaticResource RowDetailsCellWidth}" Margin="0 0 8 0" WatermarkContent="·"
                                                         Text="{Binding Trace2, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, TargetNullValue=''}"/>
                            <TextBlock Text="울음" Style="{StaticResource RowDetailsLabelStyle}"/>
                            <telerik:RadWatermarkTextBox Width="{StaticResource RowDetailsCellWidth}" Margin="0 0 8 0" WatermarkContent="·"
                                                         Text="{Binding Trace3, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, TargetNullValue=''}"/>
                            <TextBlock Text="사체" Style="{StaticResource RowDetailsLabelStyle}"/>
                            <telerik:RadWatermarkTextBox Width="{StaticResource RowDetailsCellWidth}" Margin="0 0 8 0" WatermarkContent="·"
                                                         Text="{Binding Trace4, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, TargetNullValue=''}"/>
                            <TextBlock Text="족적" Style="{StaticResource RowDetailsLabelStyle}"/>
                            <telerik:RadWatermarkTextBox Width="{StaticResource RowDetailsCellWidth}" Margin="0 0 8 0" WatermarkContent="·"
                                                         Text="{Binding Trace5, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, TargetNullValue=''}"/>
                            <TextBlock Text="털" Style="{StaticResource RowDetailsLabelStyle}"/>
                            <telerik:RadWatermarkTextBox Width="{StaticResource RowDetailsCellWidth}" Margin="0 0 8 0" WatermarkContent="·"
                                                         Text="{Binding Trace6, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, TargetNullValue=''}"/>
                            <TextBlock Text="식흔" Style="{StaticResource RowDetailsLabelStyle}"/>
                            <telerik:RadWatermarkTextBox Width="{StaticResource RowDetailsCellWidth}" Margin="0 0 8 0" WatermarkContent="·"
                                                         Text="{Binding Trace7, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, TargetNullValue=''}"/>
                            <TextBlock Text="굴" Style="{StaticResource RowDetailsLabelStyle}"/>
                            <telerik:RadWatermarkTextBox Width="{StaticResource RowDetailsCellWidth}" Margin="0 0 8 0" WatermarkContent="·"
                                                         Text="{Binding Trace8, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, TargetNullValue=''}"/>
                            <TextBlock Text="번식지" Style="{StaticResource RowDetailsLabelStyle}"/>
                            <telerik:RadWatermarkTextBox Width="{StaticResource RowDetailsCellWidth}" Margin="0 0 8 0" WatermarkContent="·"
                                                         Text="{Binding Trace9, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, TargetNullValue=''}"/>
                            <TextBlock Text="배설물" Style="{StaticResource RowDetailsLabelStyle}"/>
                            <telerik:RadWatermarkTextBox Width="{StaticResource RowDetailsCellWidth}" Margin="0 0 8 0" WatermarkContent="·"
                                                         Text="{Binding Trace10, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, TargetNullValue=''}"/>
                            <TextBlock Text="카메라" Style="{StaticResource RowDetailsLabelStyle}"/>
                            <telerik:RadWatermarkTextBox Width="{StaticResource RowDetailsCellWidth}" Margin="0 0 8 0" WatermarkContent="·"
                                                         Text="{Binding Trace11, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, TargetNullValue=''}"/>
                            <TextBlock Text="기타" Style="{StaticResource RowDetailsLabelStyle}"
                                       ToolTipService.ToolTip="기타(탐문 등)"/>
                            <telerik:RadWatermarkTextBox Width="{StaticResource RowDetailsCellWidth}" WatermarkContent="·"
                                                         Text="{Binding Trace12, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, TargetNullValue=''}"/>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ctrl:ObservationGrid.RowDetails>
        </ctrl:ObservationGrid>

        <!-- ③ 상태 · 액션 바 : 관찰형은 결과 레일을 두지 않고 총계를 여기에 싣는다(§5-1-3) -->
        <ctrl:TaxonStatusBar Grid.Row="2" Margin="0 8 0 0">
            <ctrl:TaxonStatusBar.Summary>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="{Binding TotalSpeciesCount, StringFormat=총 {0}종}" FontWeight="SemiBold"
                               Foreground="{telerik:Windows11Resource ResourceKey=PrimaryForegroundBrush}"/>
                    <TextBlock Text="{Binding TotalIndividualCount, StringFormat= · {0}개체}" Margin="4 0 0 0"
                               Foreground="{telerik:Windows11Resource ResourceKey=PrimaryForegroundBrush}"/>
                    <TextBlock Text="{Binding TotalObservations, StringFormat= · 흔적 {0}건}" Margin="4 0 0 0"
                               Foreground="{telerik:Windows11Resource ResourceKey=PrimaryForegroundBrush}"/>
                </StackPanel>
            </ctrl:TaxonStatusBar.Summary>
            <ctrl:TaxonStatusBar.Actions>
                <StackPanel Orientation="Horizontal">
                    <telerik:RadButton Command="{Binding PruneEmptyCommand}"
                                       Margin="{StaticResource ActionButtonMargin}" Padding="{StaticResource ActionButtonPadding}"
                                       Foreground="{StaticResource DangerActionForeBrush}"
                                       ToolTipService.ToolTip="국명·흔적이 모두 빈 행을 제거합니다">
                        <StackPanel Orientation="Horizontal">
                            <icon:PackIconMaterial Kind="DeleteSweepOutline" Width="14" Height="14" VerticalAlignment="Center"/>
                            <TextBlock Text="빈 행 정리" Margin="6 0 0 0"/>
                        </StackPanel>
                    </telerik:RadButton>
                    <telerik:RadButton Command="{Binding AddRowCommand}"
                                       Margin="{StaticResource ActionButtonMargin}" Padding="{StaticResource ActionButtonPadding}">
                        <StackPanel Orientation="Horizontal">
                            <icon:PackIconMaterial Kind="PlusBoxOutline" Width="14" Height="14" VerticalAlignment="Center"/>
                            <TextBlock Text="행 추가" Margin="6 0 0 0"/>
                        </StackPanel>
                    </telerik:RadButton>
                    <telerik:RadButton Command="{Binding SaveTemporaryCommand}"
                                       Margin="{StaticResource ActionButtonMargin}" Padding="{StaticResource ActionButtonPadding}">
                        <StackPanel Orientation="Horizontal">
                            <icon:PackIconMaterial Kind="ContentSaveOutline" Width="14" Height="14" VerticalAlignment="Center"/>
                            <TextBlock Text="임시 저장" Margin="6 0 0 0"/>
                        </StackPanel>
                    </telerik:RadButton>
                    <telerik:RadButton Command="{Binding ExportBulkCommand}"
                                       Margin="{StaticResource ActionButtonMargin}" Padding="{StaticResource ActionButtonPadding}"
                                       ToolTipService.ToolTip="관리자 일괄입력용(레거시 양식) · 현행 관리자 Import 취합용">
                        <StackPanel Orientation="Horizontal">
                            <icon:PackIconModern Kind="OfficeExcel" Width="14" Height="14" VerticalAlignment="Center"/>
                            <TextBlock Text="일괄입력용" Margin="6 0 0 0"/>
                        </StackPanel>
                    </telerik:RadButton>
                    <telerik:RadButton Command="{Binding ExportExcelCommand}" Padding="{StaticResource ActionButtonPadding}"
                                       Background="{StaticResource PrimaryActionBackBrush}"
                                       Foreground="{StaticResource PrimaryActionForeBrush}"
                                       ToolTipService.ToolTip="보고서·기록용 · 조사개황/출현종/출현종요약 시트">
                        <StackPanel Orientation="Horizontal">
                            <icon:PackIconModern Kind="OfficeExcel" Width="14" Height="14" VerticalAlignment="Center"/>
                            <TextBlock Text="보고서 엑셀" Margin="6 0 0 0"/>
                        </StackPanel>
                    </telerik:RadButton>
                </StackPanel>
            </ctrl:TaxonStatusBar.Actions>
        </ctrl:TaxonStatusBar>
    </Grid>
</UserControl>
