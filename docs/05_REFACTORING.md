# 5단계 — 리팩토링

> 선행: `04_FEATURE_SITE_SESSION.md` 완료. **기능이 모두 구현된 뒤에 착수한다.**
> 원칙: **기능 변경 없음.** 리팩토링 전후로 동작·계산 결과가 100% 같아야 한다.

---

## 0. 착수 전 안전장치

1. 리팩토링 시작 전 커밋 태그: `git tag before-refactor`
2. **회귀 확인용 기준 파일 생성** — 리팩토링 전에 7개 분류군 시험 데이터로 보고서·일괄입력 엑셀을 각각 내보내 `docs\_baseline\` 에 보관.
   리팩토링 후 같은 데이터로 다시 내보내 **셀 값이 동일한지 대조**한다. 이것이 이 단계의 유일한 안전망이다.

---

## 1. 중복 제거 (가장 큰 효과)

현재 7개 분류군 VM과 exporter에 같은 코드가 복제되어 있다.

### 1-1. 입력 VM 공통 기반
`ViewModels\Base\SpeciesEntryViewModelBase<TEntry, TSpecies>` (신규)
- 공통: 종 빠른추가(검색 필터·선택·Enter 추가·포커스 이벤트), `PasteRows`, `PruneEmpty`, 이상치 검증(`ValidateRows`/`WarningCount`/`WarningText`), 임시저장 상태(`StatusText`/`LastSavedTime`), 내보내기 커맨드 게이트
- 각 분류군 VM은 **고유 필드 + 고유 계산**만 남긴다.

### 1-2. 내보내기 공통 기반
- `Export\ExcelStyle.cs` — 서식 헬퍼(3단계에서 이미 생성)
- `Export\ReportExporterBase.cs` — 조사개황 시트·출현종 시트·지점 구분 컬럼 등 공통 골격
- 각 분류군 exporter는 **건강성평가 시트 내용**만 구현

### 1-3. 화면(XAML) 공통화
- 조사개황: `SurveyOverviewControl` (완료)
- **빠른추가 바**·**하단 상태바/액션 바**도 UserControl로 추출해 7개 화면에서 재사용
  → 버튼 추가·문구 변경이 한 곳에서 끝나야 한다.

---

## 2. 공용 라이브러리 `SDSM_Core` 추출

현재 계산·검증 로직이 조사자 앱 안에만 있고, 관리자에는 `GB.cs`로 **같은 로직이 중복**되어 있다.
→ 새 프로젝트 `SDSM_Core` (netstandard2.0 또는 net8.0 클래스 라이브러리)로 분리하고 **양쪽이 참조**한다.

### 2-A. 통합 시 채택할 기준 (계산식 일치 검증 결과 반영 · 2026-08-20)

`11_CALC_PARITY.md` 대조 결과(1,379건 중 불일치 12건). 근거·재현은 `_calc_parity_20260820.md`.

| 항목 | 통합 시 채택할 동작 |
|---|---|
| FAI M1~M7 구간표 · M4~M6 비율 경계 | **양쪽 동일** — 그대로 옮긴다 |
| FAI M8 비정상종 비율 | **실수 나눗셈 + `총개체수 > 0` 가드**(90_TECH_NOTES §5 결정). 관리자·조사자 **동시** 적용 |
| FAI 0종(빈 조사) 처리 | ⏸ **미정** — 관리자 37.5/D vs 조사자 null. 사용자 결정 후 확정 |
| BMI 음수 등급 | 관리자 `"Check"` 채택(데이터 이상 신호를 남긴다). 정상 오탁치(0~4)에선 발생하지 않음 |
| BMI · DI · H' · R1 · J' · 순위점수 | **양쪽 동일** — 그대로 |
| 수질 등급 8종 | **양쪽 동일** — 그대로 |
| HRI 점수·등급 | **관리자 일괄입력(=조사자) 방식** 채택. 합계 0 → 점수·등급 `null` |
| 조사불가 사유 비교 | **공백 제거 후 비교**. 관리자 편집화면의 미제거는 결함이므로 통합하며 교정 |

> ⚠ 통합 후에는 이 대조를 **회귀 테스트로 유지**한다. `GB.cs` 제거 전에 같은 케이스로 한 번 더 돌려
> 값이 완전히 같은지 확인할 것(`GB.cs`의 특이 동작을 놓치면 과거 데이터와 어긋난다).

### 2-0. 저장소가 분리된 상태에서의 배치 (결정 사항)

1단계에서 저장소를 분리한 결과, 조사자 앱이 `..\..\SDSM\SDSM_Models\SDSM_Models.csproj` 를
**상대 경로 ProjectReference로 참조**하는 크로스 저장소 의존이 드러났다.

**결정: 현행 "나란히 clone" 방식을 유지한다.** NuGet 패키지·git submodule은 도입하지 않는다.
- 이유: 유지보수 인력이 1인이고, 패키지 배포·버전 관리 비용이 얻는 것보다 크다.
- `SDSM_Core`와 `SDSM_Models`는 **관리자 저장소(`SDSM`)에 둔다.** 관리자가 원본 데이터·모델의 기준이기 때문이다.
- 조사자 앱은 지금처럼 상대 경로로 참조한다.

**대신 반드시 할 것 — 잘못 clone했을 때 원인을 바로 알 수 있게 한다.**
- 두 저장소 각각에 `README.md` 를 만들어 **폴더 구조와 나란히 clone 요구사항**을 최상단에 명시
  ```
  <상위폴더>\
    SDSM\            (관리자, 공용 모델·SDSM_Core 포함)
    SDSM_Surveyor\   (조사자)
  ```
- `SDSM_Surveyor_App.csproj` 에 참조 경로가 없을 때 **친절한 빌드 오류**를 내는 `Target` 추가
  예: `<Error Condition="!Exists('..\..\SDSM\SDSM_Models\SDSM_Models.csproj')" Text="SDSM 저장소를 같은 상위 폴더에 나란히 clone해야 합니다." />`

> 훗날 조사자 앱을 외부에 단독 배포해야 하는 상황이 오면 그때 NuGet 분리를 재검토한다. 지금은 하지 않는다.

이관 대상
- `Ecology\` 전체(FAI·BMI·다양도지수·수질등급·HRI) + 상세 결과 클래스
- `Helpers\ChosungHelper.cs`
- 종목록·기준자료·지점 로딩 규약(`SpeciesCatalog`·`ReferenceData`·`SiteCatalog`)
- 엑셀 셀 매핑 상수(`90_TECH_NOTES.md` §4를 코드 상수로)

**주의**: 관리자 `GB.cs`를 곧바로 삭제하지 말 것. `SDSM_Core` 호출로 교체하고 **결과가 같은지 확인한 뒤** 제거한다.
`GB.cs`의 정수 나눗셈 등 특이 동작은 그대로 보존해야 결과가 일치한다.

---

## 3. 경고·잔재 정리

- **CS8826 경고 21건**: `On○○Changed(T v)` → 매개변수명을 **`value`** 로 통일(소스 생성기 규약). 동작 영향 없음.
- 미사용 코드 제거: 사용되지 않는 `AddRow`/`RemoveRow` 커맨드, 남은 시드 데이터, 폐기된 필드
- `SpeciesListProvider`의 `Fallback()` 최소 시드는 유지(파일 없을 때 앱이 죽지 않게 하는 안전장치)
- 폴더 구조 정리: `Export\`, `Data\`, `Ecology\`, `Helpers\`, `Behaviors\`, `Models\`, `ViewModels\`, `Views\`

---

## 4. 엑셀 계산식 검수 (미완 과제)

조사자 제출 엑셀에 들어 있는 **수식 자체**가 내부 코드 로직과 같은 정의인지 셀 단위로 대조한다.
- 확인된 사실: 관리자는 엑셀의 지수 "값"을 무시하고 **원자료로 재계산**한다.
- 남은 확인: 반올림 방식·결측 처리·조사불가 처리까지 동일한지.
- 차이가 있으면 **지침(수생태계 건강성 평가 방법)을 기준**으로 코드를 채택하고, 차이를 `90_TECH_NOTES.md` §5에 기록한다.

---

## 완료 기준

- [ ] 7개 VM·7개 exporter의 중복 코드가 공통 기반으로 통합됨
- [ ] `SDSM_Core` 분리, 관리자·조사자가 동일 계산 코드 사용
- [ ] 리팩토링 전후 **내보내기 엑셀 셀 값이 동일**(`docs\_baseline\` 대조 통과)
- [ ] CS8826 경고 0건, 빌드 경고 최소화
- [ ] 빌드 통과 + 7개 탭 실행 확인
